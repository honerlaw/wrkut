services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: wrkut
      POSTGRES_PASSWORD: wrkut
      POSTGRES_DB: wrkut
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
